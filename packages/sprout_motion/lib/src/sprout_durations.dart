class SproutDurations {
  const SproutDurations._();

  static const buttonPress = Duration(milliseconds: 100);
  static const cardEntrance = Duration(milliseconds: 300);
  static const pageTransition = Duration(milliseconds: 260);
  static const progressRing = Duration(milliseconds: 850);
  static const xpReward = Duration(milliseconds: 600);
  static const mascotReaction = Duration(milliseconds: 1400);
  static const confetti = Duration(milliseconds: 1200);
}
