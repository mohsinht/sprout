name: sprout_motion
description: Shared motion primitives for the Sprout Flutter app.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.2
  lottie: ^3.3.1
  rive: ^0.13.20

dev_dependencies:
  flutter_lints: ^5.0.0
